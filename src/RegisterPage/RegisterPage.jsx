import React from 'react';
import { Link } from 'react-router-dom';
import { connect } from 'react-redux';

import { userActions } from '../_actions';

class RegisterPage extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            user: {
                firstName: '',
                lastName: '',
                username: '',
                password: '',
                standard: '',
                email: '',
                mobileNumber: '',
                roles: 'ROLE_USER'
            },
            submitted: false
        };

        this.handleChange = this.handleChange.bind(this);
        this.handleSubmit = this.handleSubmit.bind(this);
        this.handleClick = this.handleClick.bind(this);
    }

    handleClick(event) {
      if(event.target.name === 'reset') {
        this.setState(
            {
              user: {
                firstName: '',
                lastName: '',
                username: '',
                password: '',
                standard: '',
                email: '',
                mobileNumber: '',
                roles: 'ROLE_USER'
            },
            submitted: false
        });
      }
    }

    handleChange(event) {
        const { name, value } = event.target;
        const { user } = this.state;
        console.log("event " + (event.target.name));
        if(event.target.name === 'roles') {
          user.roles = event.target.value;
        }

        this.setState({
            user: {
                ...user,
                [name]: value
            }
        });
    }

    handleSubmit(event) {
        event.preventDefault();
        this.setState({ submitted: true });
        const { user } = this.state;
        console.log("roles " + JSON.stringify(user));
        user.roles = [user.roles];
        const { dispatch } = this.props;
        if (user.firstName && user.lastName && user.username && user.password) {
            dispatch(userActions.register(user));
        }
    }

    render() {
        const { registering  } = this.props;
        const { user, submitted } = this.state;
        return (
            <div className="col-md-6 col-md-offset-3">
                <h2>Register User</h2>
                <form name="form" onSubmit={this.handleSubmit}>
                    <div className={'form-group' + (submitted && !user.firstName ? ' has-error' : '')}>
                        <label htmlFor="firstName">First Name</label>

                        <input type="text" className="form-control" name="firstName" value={user.firstName} onChange={this.handleChange} />
                        {submitted && !user.firstName &&
                            <div className="help-block">First Name is required</div>
                        }
                    </div>
                    <div className={'form-group' + (submitted && !user.lastName ? ' has-error' : '')}>
                        <label htmlFor="lastName">Last Name</label>
                        <input type="text" className="form-control" name="lastName" value={user.lastName} onChange={this.handleChange} />
                        {submitted && !user.lastName &&
                            <div className="help-block">Last Name is required</div>
                        }
                    </div>
                    <div className={'form-group' + (submitted && !user.username ? ' has-error' : '')}>
                        <label htmlFor="username">Username</label>
                        <input type="text" className="form-control" name="username" value={user.username} onChange={this.handleChange} />
                        {submitted && !user.username &&
                            <div className="help-block">Username is required</div>
                        }
                    </div>
                    <div className={'form-group' + (submitted && !user.password ? ' has-error' : '')}>
                        <label htmlFor="password">Password</label>
                        <input type="password" className="form-control" name="password" value={user.password} onChange={this.handleChange} />
                        {submitted && !user.password &&
                            <div className="help-block">Password is required</div>
                        }
                    </div>
                    <div className={'form-group' + (submitted && !user.standard ? ' has-error' : '')}>
                        <label htmlFor="standard">Standard</label>
                        <input type="text" className="form-control" name="standard" value={user.standard} onChange={this.handleChange} />
                        {submitted && !user.standard &&
                            <div className="help-block">Standard is required</div>
                        }
                    </div>
                    <div className={'form-group' + (submitted && !user.email ? ' has-error' : '')}>
                        <label htmlFor="email">Email</label>
                        <input type="email" className="form-control" name="email" value={user.email} onChange={this.handleChange} />
                        {submitted && !user.email &&
                            <div className="help-block">Email is required</div>
                        }
                    </div>
                    <div className={'form-group' + (submitted && !user.mobileNumber ? ' has-error' : '')}>
                        <label htmlFor="mobileNumber">Mobile Number</label>
                        <input type="mobileNumber" className="form-control" name="mobileNumber" value={user.mobileNumber} onChange={this.handleChange} />
                        {submitted && !user.mobileNumber &&
                            <div className="help-block">Mobile Number is required</div>
                        }
                    </div>

                    <div className={'form-group' + (submitted && !user.roles ? ' has-error' : '')}>
                        <label htmlFor="roles">Roles</label>
                        <select value={user.roles} onChange={this.handleChange}  className="form-control" name="roles">
                          <option value="ROLE_USER">User</option>
                          <option value="ROLE_ADMIN">Admin</option>
                        </select>
                        {submitted && !user.roles &&
                            <div className="help-block">Roles is required</div>
                        }
                    </div>
                    <div className="form-group">
                        <button className="btn btn-primary">Register</button>
                        {registering &&
                            <img src="data:image/gif;base64," />
                        }
                        <Link to="/login" className="btn btn-link">Cancel</Link>
                        <Link to="/register" className="btn btn-link" onClick={this.handleClick} name="reset">Reset</Link>
                    </div>
                </form>
            </div>
        );
    }
}

function mapStateToProps(state) {
    const { registering } = state.registration;
    return {
        registering
    };
}

const connectedRegisterPage = connect(mapStateToProps)(RegisterPage);
export { connectedRegisterPage as RegisterPage };
